export interface UserLogin {
    name: string,
    password: string
}

export interface UserModel {
    id: number,
    userName: string,
    name: string,
    lastName: string,
    birthDate: string
}